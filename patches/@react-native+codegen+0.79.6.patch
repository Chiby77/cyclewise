diff --git a/node_modules/@react-native/codegen/lib/parsers/flow/components/componentsUtils.js b/node_modules/@react-native/codegen/lib/parsers/flow/components/componentsUtils.js
index 79b7cf4..5df3a3a 100644
--- a/node_modules/@react-native/codegen/lib/parsers/flow/components/componentsUtils.js
+++ b/node_modules/@react-native/codegen/lib/parsers/flow/components/componentsUtils.js
@@ -41,7 +41,7 @@ function getTypeAnnotationForArray(
   if (extractedTypeAnnotation.type === 'GenericTypeAnnotation') {
     // Resolve the type alias if it's not defined inline
     const objectType = getValueFromTypes(extractedTypeAnnotation, types);
-    if (objectType.id.name === '$ReadOnly') {
+    if (objectType.id.name === '$ReadOnly' || objectType.id.name === 'Readonly') {
       return {
         type: 'ObjectTypeAnnotation',
         properties: flattenProperties(
@@ -53,7 +53,7 @@ function getTypeAnnotationForArray(
           .filter(Boolean),
       };
     }
-    if (objectType.id.name === '$ReadOnlyArray') {
+    if (objectType.id.name === '$ReadOnlyArray' || objectType.id.name === 'ReadonlyArray') {
       // We need to go yet another level deeper to resolve
       // types that may be defined in a type alias
       const nestedObjectType = getValueFromTypes(
@@ -210,7 +210,8 @@ function getTypeAnnotation(
   const typeAnnotation = getValueFromTypes(annotation, types);
   if (
     typeAnnotation.type === 'GenericTypeAnnotation' &&
-    parser.getTypeAnnotationName(typeAnnotation) === '$ReadOnlyArray'
+    (parser.getTypeAnnotationName(typeAnnotation) === '$ReadOnlyArray' ||
+      parser.getTypeAnnotationName(typeAnnotation) === 'ReadonlyArray')
   ) {
     return {
       type: 'ArrayTypeAnnotation',
@@ -226,7 +227,8 @@ function getTypeAnnotation(
   }
   if (
     typeAnnotation.type === 'GenericTypeAnnotation' &&
-    parser.getTypeAnnotationName(typeAnnotation) === '$ReadOnly'
+    (parser.getTypeAnnotationName(typeAnnotation) === '$ReadOnly' ||
+      parser.getTypeAnnotationName(typeAnnotation) === 'Readonly')
   ) {
     return {
       type: 'ObjectTypeAnnotation',
diff --git a/node_modules/@react-native/codegen/lib/parsers/flow/components/events.js b/node_modules/@react-native/codegen/lib/parsers/flow/components/events.js
index 6e3b854..a9deed2 100644
--- a/node_modules/@react-native/codegen/lib/parsers/flow/components/events.js
+++ b/node_modules/@react-native/codegen/lib/parsers/flow/components/events.js
@@ -170,7 +170,7 @@ function findEventArgumentsAndType(
 ) {
   throwIfEventHasNoName(typeAnnotation, parser);
   const name = parser.getTypeAnnotationName(typeAnnotation);
-  if (name === '$ReadOnly') {
+  if (name === '$ReadOnly' || name === 'Readonly') {
     return {
       argumentProps: typeAnnotation.typeParameters.params[0].properties,
       paperTopLevelNameDeprecated: paperName,
diff --git a/node_modules/@react-native/codegen/lib/parsers/typescript/components/commands.js b/node_modules/@react-native/codegen/lib/parsers/typescript/components/commands.js
index 531bd56..d7254c0 100644
--- a/node_modules/@react-native/codegen/lib/parsers/typescript/components/commands.js
+++ b/node_modules/@react-native/codegen/lib/parsers/typescript/components/commands.js
@@ -31,7 +31,11 @@ function buildCommandSchemaInternal(name, optional, parameters, types) {
       ((_firstParam$typeAnnot2 = firstParam.typeAnnotation.typeName.right) ===
         null || _firstParam$typeAnnot2 === void 0
         ? void 0
-        : _firstParam$typeAnnot2.name) === 'ElementRef'
+        : _firstParam$typeAnnot2.name) === 'ElementRef' ||
+      ((_firstParam$typeAnnot2 = firstParam.typeAnnotation.typeName.right) ===
+        null || _firstParam$typeAnnot2 === void 0
+        ? void 0
+        : _firstParam$typeAnnot2.name) === 'ComponentRef'
     )
   ) {
     throw new Error(
diff --git a/node_modules/@react-native/codegen/lib/parsers/typescript/components/componentsUtils.js b/node_modules/@react-native/codegen/lib/parsers/typescript/components/componentsUtils.js
index 3ba47fe..c5a2087 100644
--- a/node_modules/@react-native/codegen/lib/parsers/typescript/components/componentsUtils.js
+++ b/node_modules/@react-native/codegen/lib/parsers/typescript/components/componentsUtils.js
@@ -294,16 +294,14 @@ function getTypeAnnotationForArray(
     return arrayType;
   }
   const type =
-    extractedTypeAnnotation.elementType === 'TSTypeReference'
-      ? extractedTypeAnnotation.elementType.typeName.name
+    extractedTypeAnnotation.elementType &&
+    extractedTypeAnnotation.elementType.type === 'TSTypeReference'
+      ? parser.getTypeAnnotationName(extractedTypeAnnotation.elementType)
       : ((_extractedTypeAnnotat = extractedTypeAnnotation.elementType) ===
           null || _extractedTypeAnnotat === void 0
           ? void 0
           : _extractedTypeAnnotat.type) ||
-        ((_extractedTypeAnnotat2 = extractedTypeAnnotation.typeName) === null ||
-        _extractedTypeAnnotat2 === void 0
-          ? void 0
-          : _extractedTypeAnnotat2.name) ||
+        parser.getTypeAnnotationName(extractedTypeAnnotation) ||
         extractedTypeAnnotation.type;
   const common = getCommonTypeAnnotation(
     name,
diff --git a/node_modules/@react-native/codegen/lib/parsers/typescript/parseTopLevelType.js b/node_modules/@react-native/codegen/lib/parsers/typescript/parseTopLevelType.js
index 0ef998d..4ab03a9 100644
--- a/node_modules/@react-native/codegen/lib/parsers/typescript/parseTopLevelType.js
+++ b/node_modules/@react-native/codegen/lib/parsers/typescript/parseTopLevelType.js
@@ -67,6 +67,13 @@ function evaluateLiteral(literalNode) {
     'The default value in WithDefault must be string, number, boolean or null .',
   );
 }
+function getTypeName(typeName) {
+  if (!typeName) return undefined;
+  if (typeName.type === 'TSQualifiedName') {
+    return typeName.right ? typeName.right.name : undefined;
+  }
+  return typeName.name;
+}
 function handleUnionAndParen(type, result, knownTypes) {
   switch (type.type) {
     case 'TSParenthesizedType': {
@@ -89,9 +96,10 @@ function handleUnionAndParen(type, result, knownTypes) {
       break;
     }
     case 'TSTypeReference':
-      if (type.typeName.name === 'Readonly') {
+      const name = getTypeName(type.typeName);
+      if (name === 'Readonly') {
         handleUnionAndParen(type.typeParameters.params[0], result, knownTypes);
-      } else if (type.typeName.name === 'WithDefault') {
+      } else if (name === 'WithDefault') {
         if (result.optional) {
           throw new Error(
             'WithDefault<> is optional and does not need to be marked as optional. Please remove the union of undefined and/or null',
@@ -116,7 +124,7 @@ function handleUnionAndParen(type, result, knownTypes) {
         const resolvedType = getValueFromTypes(type, knownTypes);
         if (
           resolvedType.type === 'TSTypeReference' &&
-          resolvedType.typeName.name === type.typeName.name
+          getTypeName(resolvedType.typeName) === getTypeName(type.typeName)
         ) {
           result.unions.push(type);
         } else {
@@ -166,13 +174,14 @@ function handleIntersectionAndParen(type, result, knownTypes) {
       break;
     }
     case 'TSTypeReference':
-      if (type.typeName.name === 'Readonly') {
+      const name = getTypeName(type.typeName);
+      if (name === 'Readonly') {
         handleIntersectionAndParen(
           type.typeParameters.params[0],
           result,
           knownTypes,
         );
-      } else if (type.typeName.name === 'WithDefault') {
+      } else if (name === 'WithDefault') {
         throw new Error('WithDefault<> is now allowed in intersection types.');
       } else if (!knownTypes) {
         result.push(type);
@@ -180,7 +189,7 @@ function handleIntersectionAndParen(type, result, knownTypes) {
         const resolvedType = getValueFromTypes(type, knownTypes);
         if (
           resolvedType.type === 'TSTypeReference' &&
-          resolvedType.typeName.name === type.typeName.name
+          getTypeName(resolvedType.typeName) === getTypeName(type.typeName)
         ) {
           result.push(type);
         } else {
diff --git a/node_modules/@react-native/codegen/lib/parsers/typescript/parser.js b/node_modules/@react-native/codegen/lib/parsers/typescript/parser.js
index c351766..8d9cb65 100644
--- a/node_modules/@react-native/codegen/lib/parsers/typescript/parser.js
+++ b/node_modules/@react-native/codegen/lib/parsers/typescript/parser.js
@@ -100,13 +100,13 @@ class TypeScriptParser {
     return 'TypeScript';
   }
   getTypeAnnotationName(typeAnnotation) {
-    var _typeAnnotation$typeN;
-    return typeAnnotation === null ||
-      typeAnnotation === void 0 ||
-      (_typeAnnotation$typeN = typeAnnotation.typeName) === null ||
-      _typeAnnotation$typeN === void 0
-      ? void 0
-      : _typeAnnotation$typeN.name;
+    if (!typeAnnotation) return undefined;
+    const typeName = typeAnnotation.typeName;
+    if (!typeName) return undefined;
+    if (typeName.type === 'TSQualifiedName') {
+      return typeName.right ? typeName.right.name : undefined;
+    }
+    return typeName.name;
   }
   checkIfInvalidModule(typeArguments) {
     return (
